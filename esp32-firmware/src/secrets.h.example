#pragma once

// Copier ce fichier en secrets.h et renseigner vos valeurs locales.
// secrets.h est ignoré par git — ne pas committer de mots de passe.
//
// WiFi : l'ESP32 ne supporte que le 2.4 GHz et le WPA2 personnel (pas WPA2-Enterprise
// type Campus-Invite). SSID avec apostrophe : "iPhone d'Aleks" (guillemets doubles).

#define WIFI_SSID "votre_ssid"
#define WIFI_PASSWORD "votre_mot_de_passe"

#define MQTT_BROKER "192.168.1.100"
#define MQTT_PORT 1883
#define MQTT_USER ""
#define MQTT_PASSWORD ""

// Optionnel : jeton pour proteger l'API (laisser vide = dashboard sans auth).
#define API_TOKEN ""
